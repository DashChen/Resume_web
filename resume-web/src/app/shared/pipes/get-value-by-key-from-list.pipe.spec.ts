import { GetValueByKeyFromListPipe } from './get-value-by-key-from-list.pipe';

describe('GetValueByKeyFromListPipe', () => {
  it('create an instance', () => {
    const pipe = new GetValueByKeyFromListPipe();
    expect(pipe).toBeTruthy();
  });
});
